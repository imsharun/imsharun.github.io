# Production public Razorpay key (public key id only)
# Set this in CI or hosting env instead of committing when possible
VITE_RAZORPAY_KEY=rzp_live_YOUR_LIVE_KEY
# Production backend base URL (set in CI/hosting instead of committing when possible)
VITE_API_BASE_URL=https://api.example.com
